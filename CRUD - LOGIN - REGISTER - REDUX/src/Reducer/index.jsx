import ChangeNumber from "./ChangeNumber";
import { combineReducers } from "redux";

const RootReducer = combineReducers({
    ChangeNumber //You can also add multiple Function here with , (this coma)

})

export default RootReducer;